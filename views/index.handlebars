<header>
  <div class="title m-3">
    <h1 class="text-center">Devour The Burger</h1>
    <p class="text-center">Devour Your Desired Burger or Craft Your Own to Devour</p>
  </div>
</header>

<div class="container-main">
  <div class="row">
    <div class="burger-cards d-flex">

      <div class="card justify-content-center m-3 col-sm-12 col-md-5">
        <div class="card-body">
          <h2 class="card-title">Burgers to be Devoured</h2>
          <ul>
            {{#each burgers}}
            {{#unless devoured}}
            {{> burger-block devoured=false}}
            {{/unless}}
            {{/each}}
          </ul>
        </div>
      </div>

      <div class="card form-card justify-content-center m-3 p-3 col-sm-12 col-md-2">
        <form class="form-group burger-input">
          <label for="addingNewBurger">Create A Burger</label>
          <input class="form-control">
          <button class="btn btn-outline-warning burgerBtn" type="submit">Add Your Burger</button>
        </form>
      </div>

      <div class="card justify-content-center m-3 col-sm-12 col-md-5">
        <div class="card-body">
          <h2 class="card-title">Devoured Burgers</h2>
          <ul>
            {{#each burgers}}
            {{#if burgers}}
            {{> burger-block devoured=true}}
            {{/if}}
            {{/each}}
          </ul>
        </div>
      </div>

    </div>
  </div>
</div>